version: '3.8'
services:
  summarization-api:
    build: .
    ports: ["8000:8000"]
    environment: [ "ENVIRONMENT=production" ]
    volumes: ["./models:/app/models", "./logs:/app/logs"]
    restart: unless-stopped
